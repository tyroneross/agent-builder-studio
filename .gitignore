node_modules/
.next/
.DS_Store
.build-loop/
.rally/
.navgator/
.bookmark/
.claude/
.ibr/
# Studio-local tool-registry state (workspace discovery + external
# registrations); regenerated on demand, never committed.
.agent-studio/
*.log
__pycache__/

# Local artifact staging — generated output (packages/skills/plugins/agents)
# lives here, tracked in .artifacts/registry.json, until promoted to a standalone
# live folder. Working output, never committed.
.artifacts/
