{
  "name": "meetings",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Meeting transcript analyzer — local upload → extract → chunk → SQLite + knowledge graph → search. Extracted from the agent-builder workbench into its own app.",
  "scripts": {
    "dev": "next dev --webpack -p 3032",
    "build": "next build",
    "start": "next start -p 3032",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "lucide-react": "^1.11.0",
    "next": "^16.2.4",
    "react": "^19.2.5",
    "react-dom": "^19.2.5"
  },
  "license": "Apache-2.0"
}
